const initialState = {
  list: ["u2", "queen", "pink floyd"],
};

const artistReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default artistReducer;
