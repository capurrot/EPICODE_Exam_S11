const initialState = {
  list: ["u2", "queen", "pink floyd"],
};

const songsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default songsReducer;
