const initialState = {
  list: ["u2", "queen", "pink floyd"],
};

const artistsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default artistsReducer;
